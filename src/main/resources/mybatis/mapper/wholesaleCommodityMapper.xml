<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dian.mmall.dao.releaseDao.WholesaleCommodityMapper">
     
     <!--发布过的条数  -->
     <select id="countNum" resultType="int">
        select count(*) from shichang_wholesaleCommodity where welfareStatus!=3  and releaseType=#{releaseType} and userId =#{userId}
    </select>
         
          <!--创建用户成功返回非0  -->
     <insert id="create_wholesaleCommodity"  >
        insert into shichang_wholesaleCommodity (
        userId,releaseType,updateTime,createTime,pictureUrl,contact,
        consigneeName,detailed,realNameId,userType,serviceDetailed,
        remarks,
        serviceIntroduction,releaseTitle,authentiCationStatus,welfareStatus,
        serviceAndprice,serviceType
        ) values (
            #{userId},  #{releaseType}, #{updateTime}, #{createTime},#{pictureUrl},
            #{contact},#{consigneeName},#{detailed},
            #{realNameId},#{userType},
            #{serviceDetailed},
            #{remarks},#{serviceIntroduction},
            #{releaseTitle},#{authentiCationStatus},#{welfareStatus},
            #{serviceAndprice},#{serviceType}
        )
    </insert><!-- countNum -->
    
    
  
    
</mapper>